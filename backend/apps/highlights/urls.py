"""URL configuration for highlights app."""
from django.urls import path
from . import views

urlpatterns = [
    # Highlight endpoints are accessed via /v1/chapters/{id}/highlights
    # These will be added to the main URL config
]
