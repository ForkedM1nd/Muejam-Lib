# ── Environment Configuration ──

# Environment (development, staging, production)
# Automatically set by Vite based on --mode flag
# MODE=development

# ── API Configuration ──
VITE_API_BASE_URL=http://localhost:8000/v1

# ── WebSocket Configuration ──
VITE_WS_URL=ws://localhost:8000/ws

# ── Authentication ──
# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=your-clerk-publishable-key

# Clerk URLs (optional - defaults are used if not set)
VITE_CLERK_SIGN_IN_URL=/sign-in
VITE_CLERK_SIGN_UP_URL=/sign-up
VITE_CLERK_AFTER_SIGN_IN_URL=/discover
VITE_CLERK_AFTER_SIGN_UP_URL=/discover

# ── Security ──
# reCAPTCHA v3 (optional - bot protection for content submission)
VITE_RECAPTCHA_SITE_KEY=your-recaptcha-site-key

# ── Push Notifications ──
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key

# ── Feature Flags ──
# Set to "false" to disable features (enabled by default)
VITE_ENABLE_PUSH_NOTIFICATIONS=true
VITE_ENABLE_OFFLINE_MODE=true
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_WEBSOCKET=true
VITE_ENABLE_SERVICE_WORKER=true

# ── A/B Testing / Experiments ──
# Format: experiment1:true,experiment2:false
# VITE_EXPERIMENTS=newDesign:true,betaFeature:false

# ── Monitoring ──
VITE_ENABLE_SENTRY=false
# VITE_SENTRY_DSN=your-sentry-dsn
VITE_ENABLE_PERFORMANCE_MONITORING=false
